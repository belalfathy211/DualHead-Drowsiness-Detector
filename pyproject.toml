[project]
name = "twohead"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pillow>=12.2.0",
    "pytest>=9.0.3",
    "ruff>=0.15.12",
    "torch>=2.11.0",
    "torchvision>=0.26.0",
]
[tool.pytest.ini_options]
pythonpath = ["."]